import { createStore } from 'vuex'
import { TestModule } from "@/store/TestModules/TestModule";
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
      TestModule: TestModule
  }
})
